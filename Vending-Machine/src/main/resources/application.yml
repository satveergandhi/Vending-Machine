server:
  port: 9092
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vending_machine?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl.auto: create
